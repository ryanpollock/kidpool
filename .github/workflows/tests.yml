name: Tests

on:
  pull_request:
    branches: [main, staging]
  push:
    branches: [main, staging]

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  static-and-integration:
    name: Static + Integration Tests (Local Supabase)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start local Supabase stack
        run: supabase start

      - name: Apply migrations + seed
        run: supabase db reset

      - name: Run static contract tests
        run: npm run check:contracts

      - name: Run integration tests (local)
        run: npm run test:integration:local

      - name: Build (for sites worker test)
        run: npm run build

      - name: Run sites worker test
        run: npm run test:sites

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Mobile runtime integrity check
        run: npm run check:runtime

  playwright:
    name: Playwright E2E Tests (Local Supabase)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start local Supabase stack
        run: supabase start

      - name: Apply migrations + seed
        run: supabase db reset

      - name: Run Playwright tests (mobile runtime — no DB needed)
        run: npx playwright test mobile-runtime.spec.ts
        env:
          TEST_DB_TARGET: local

      - name: Upload test results on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-results
          path: test-results/
          retention-days: 7